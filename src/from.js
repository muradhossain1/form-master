/**
 *  1. use form teg and onSubmit={} handler with event (e), From e access e.target.[name of the input field].valu
 * 
 * 
 *  2. controlled element : use individual field state for each input field(useState & onChange() handler use)
 * 
 * 
 *  3. controlled element : one object in a state holding al the inpur fields value.
 * 
 * 
 *  4. Uncontrolled element : useRef to create a reference to the element and access value by using like : nameRef.current.value
 * 
 * 
 *  5. Use hook to handle states and submit and error. example react hook form 
 * 
 * 
 *  6. 
 */